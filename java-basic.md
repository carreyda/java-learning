# Java 基础完整笔记（零基础入门）

## 一、Java 概述

### 1\.1 Java 简介

Java 是一种**面向对象、跨平台、可移植、高性能**的编程语言，由 Sun 公司（现 Oracle）推出，核心口号：**一次编写，到处运行（Write Once, Run Anywhere）**。

### 1\.2 Java 三大版本

- **Java SE（标准版）**：基础核心，桌面程序、基础语法，是 Java 其他版本的基础

- **Java EE（企业版）**：Web开发、后端服务、分布式项目开发

- **Java ME（微型版）**：移动端、嵌入式设备开发（现已淘汰）

### 1\.3 跨平台原理

Java 源代码\(\.java\) → 编译生成字节码文件\(\.class\) → 由**JVM（Java虚拟机）**解析执行。

不同操作系统拥有对应版本的 JVM，字节码文件可在任意 JVM 上运行，实现跨平台。

### 1\.4 JDK、JRE、JVM 区别

- **JVM**：Java虚拟机，运行字节码的容器，Java跨平台的核心

- **JRE**：Java运行环境，包含 JVM \+ 核心类库，仅用于运行Java程序

- **JDK**：Java开发工具包，包含 JRE \+ 编译、调试等开发工具，**开发必备**

包含关系：**JDK \> JRE \> JVM**

### 1\.5环境安装
JDK下载： https://www.oracle.com

IDE下载： www.jetbrains.com.cn

## 二、Java 入门程序

### 2\.1 第一个程序 HelloWorld

```java
// 公共类，类名必须和文件名一致
public class HelloWorld {
    // 主方法：程序入口，固定写法
    public static void main(String[] args) {
        // 控制台输出语句
        System.out.println("Hello Java!");
    }
}
```

### 2\.2 程序执行流程

1. 编写 `.java` 源代码文件

2. 编译：`javac 文件名.java`，生成 `.class` 字节码文件

3. 运行：`java 类名`，JVM 解析执行字节码

### 2\.3 基础语法规范

- Java 严格**区分大小写**

- 语句以**分号 ;** 结尾

- 公共类名必须与文件名完全一致

- 代码缩进规范，使用大括号 `{}` 包裹代码块

## 三、Java 变量与数据类型

### 3\.1 变量概念

变量是内存中临时存储数据的容器，值可随时修改，定义格式：`数据类型 变量名 = 初始值;`

### 3\.2 八大基本数据类型

Java 数据类型分为：**基本数据类型**、**引用数据类型**

|类型分类|数据类型|字节数|取值范围|默认值|
|---|---|---|---|---|
|整数型|byte|1|\-128 \~ 127|0|
||short|2|\-32768 \~ 32767|0|
||int（默认）|4|\-2147483648 \~ 2147483647|0|
||long|8|超大整数范围|0L|
|浮点型|float|4|单精度小数|0\.0F|
||double（默认）|8|双精度小数|0\.0|
|字符型|char|2|单个字符|' \\u0000 '|
|布尔型|boolean|1|true / false|false|

### 3\.3 引用数据类型

存储对象的内存地址，而非具体数值，常见：**String、数组、类、接口**，默认值：`null`

### 3\.4 数据类型转换

#### 1\. 自动转换（隐式转换）

小类型 → 大类型，自动执行，无精度丢失

顺序：`byte < short < int < long < float < double`

#### 2\. 强制转换（显式转换）

大类型 → 小类型，手动强制转换，**可能丢失精度**

```java
double a = 3.9;
int b = (int) a; // 强制转换，结果为3，小数部分丢失
```

## 四、运算符

### 4\.1 算术运算符

`+、-、*、/、%、++、--`

- 整数相除结果为整数，小数相除结果为小数

- `%` 取模（取余数），常用于判断奇偶、整除

- 后置\+\+：先取值后自增；前置\+\+：先自增后取值

### 4\.2 赋值运算符

`=、+=、-=、*=、/=、%=`

复合赋值运算符会自动强制类型转换

### 4\.3 比较运算符

`>、<、>=、<=、==、!=`

结果均为 **boolean 类型（true/false）**，注意：比较相等是 `==`，单个 `=` 是赋值

### 4\.4 逻辑运算符

用于连接布尔表达式，多用于条件判断

- `&`\& 短路与：全真为真，左边为假则右边不执行

- `||` 短路或：全假为假，左边为真则右边不执行

- `!` 非：取反

### 4\.5 三元运算符

格式：`条件 ? 表达式1 : 表达式2`

条件为true执行表达式1，为false执行表达式2

```java
int max = 10 > 5 ? 10 : 5; // 结果为10
```

## 五、流程控制语句

### 5\.1 分支语句

#### 1\. if\-else 分支

```java
// 单分支
if(条件){
    代码块;
}

// 双分支
if(条件){
    满足条件执行;
}else{
    不满足条件执行;
}

// 多分支
if(条件1){
}else if(条件2){
}else{
}
```

#### 2\. switch 分支

适用于固定值匹配，支持 byte、short、int、char、String、枚举

```java
switch(变量){
    case 值1:
        代码;
        break; // 防止穿透
    case 值2:
        代码;
        break;
    default:
        默认代码;
}
```

### 5\.2 循环语句

#### 1\. for 循环（常用）

适合已知循环次数的场景

```java
for(初始化变量; 循环条件; 迭代更新){
    循环体代码;
}
```

#### 2\. while 循环

先判断后执行，适合未知循环次数

```java
while(循环条件){
    循环体;
    迭代更新;
}
```

#### 3\. do\-while 循环

先执行后判断，**至少执行一次**

```java
do{
    循环体;
}while(循环条件);
```

### 5\.3 循环控制关键字

- **break**：跳出当前循环/switch，终止循环

- **continue**：跳过本次循环，直接进入下一次循环

## 六、数组

### 6\.1 数组概念

数组是**相同数据类型**的有序集合，长度固定，属于引用数据类型

### 6\.2 数组定义与初始化

```java
// 1. 动态初始化：指定长度
int[] arr1 = new int[5];

// 2. 静态初始化：直接赋值
int[] arr2 = {1,2,3,4,5};
int[] arr3 = new int[]{1,2,3};
```

### 6\.3 数组常见操作

- 获取数组长度：`数组名.length`

- 遍历数组：配合 for 循环，通过下标取值（下标从 0 开始）

- 数组默认值：整数0、小数0\.0、字符空字符、布尔false、引用null

### 6\.4 常见异常

- **数组下标越界异常**：访问下标超出 0\~length\-1 范围

- **空指针异常**：数组未初始化，直接调用

## 七、方法

### 7\.1 方法概念

方法是封装一段重复代码的代码块，提高代码复用性、可读性

### 7\.2 方法定义格式

```java
修饰符 返回值类型 方法名(参数列表){
    方法体代码;
    return 返回值;
}
```

- 返回值类型：无返回值写 `void`

- 方法名：小驼峰命名，见名知意

- 参数列表：可选，用于接收外部数据

### 7\.3 方法重载

同一个类中，多个方法**方法名相同，参数列表不同（个数、类型、顺序）**，与返回值无关

作用：统一方法名，适配不同参数场景

## 八、面向对象核心（OOP）

Java 是纯面向对象语言，三大核心特性：**封装、继承、多态**

### 8\.1 类与对象

- **类**：模板、抽象概念，包含属性（成员变量）、行为（成员方法）

- **对象**：类的实例、具体存在的个体

创建对象：`类名 对象名 = new 类名();`

### 8\.2 封装

隐藏对象的属性和实现细节，仅对外提供公共访问方式

#### 实现步骤：

1. 使用 `private` 修饰成员变量（私有化）

2. 提供`getter/setter` 方法访问、修改属性

作用：保护数据安全、降低代码耦合度

### 8\.3 构造方法

创建对象时自动执行的方法，用于初始化对象属性

- 方法名**与类名完全一致**，无返回值类型

- 默认无参构造：编译器自动生成

- 自定义有参构造后，默认无参构造会失效

### 8\.4 继承

子类继承父类的属性和方法，实现代码复用，使用 `extends` 关键字

- Java **单继承**：一个子类只能有一个直接父类

- `super`：代表父类对象，可调用父类属性、方法、构造

- 方法重写：子类重写父类非私有方法，方法名、参数、返回值一致

### 8\.5 多态

同一行为，不同对象有不同表现形式

#### 实现条件：

1. 继承关系

2. 方法重写

3. 父类引用指向子类对象

核心：**编译看左边，运行看右边**

## 九、权限修饰符

|修饰符|本类|同包|不同包子类|不同包无关类|
|---|---|---|---|---|
|private（私有的）|✅|❌|❌|❌|
|default（默认）|✅|✅|❌|❌|
|protected（受保护的）|✅|✅|✅|❌|
|public（公共的）|✅|✅|✅|✅|

## 十、常用基础类

### 10\.1 String 字符串

字符串是常量，一旦创建不可修改，底层是 char 数组

#### 常用方法：

- `length()`：获取字符串长度

- `equals()`：比较字符串内容是否相同

- `substring()`：截取字符串

- `trim()`：去除首尾空格

- `split()`：字符串分割

### 10\.2 Scanner 键盘录入

用于接收用户控制台输入数据

```java
// 1. 导入包
import java.util.Scanner;
// 2. 创建对象
Scanner sc = new Scanner(System.in);
// 3. 接收数据
int num = sc.nextInt();
String str = sc.next();
```

## 十一、异常基础

### 11\.1 异常概念

程序运行中出现的不正常情况，会导致程序终止

### 11\.2 异常分类

- **编译时异常**：必须处理，否则程序无法编译

- **运行时异常**：运行期间抛出，可选择性处理

### 11\.3 异常处理方式

#### 1\. try\-catch 捕获异常

```java
try{
    // 可能出现异常的代码
}catch(异常类型 e){
    // 异常处理逻辑
}finally{
    // 无论是否异常，都会执行（释放资源）
}
```

#### 2\. throws 抛出异常

在方法上声明异常，交由调用者处理，自身不捕获异常，适合统一异常处理场景

```java
import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;

public class ExceptionDemo {
    // 方法通过throws声明编译时异常，交由调用者处理
    public static void parseDate(String dateStr) throws ParseException {
        SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
        // 该方法会抛出ParseException编译异常
        Date date = sdf.parse(dateStr);
        System.out.println("解析成功：" + date);
    }

    public static void main(String[] args) {
        try {
            // 调用声明异常的方法，必须处理异常
            parseDate("2026-06-04");
            parseDate("2026/06/04"); // 格式错误，触发异常
        } catch (ParseException e) {
            // 调用者捕获并处理异常
            System.out.println("日期解析失败：格式错误");
            e.printStackTrace();
        }
    }
}
```

**核心特点**：
1\. throws 写在方法签名后，可声明多个异常，用逗号分隔
2\. 仅做异常声明，不处理异常，把异常责任抛给方法调用者
3\. 声明编译时异常后，调用者必须主动处理（try\-catch 或继续 throws抛出）



> （注：文档部分内容可能由 AI 生成）
